apiVersion: v2
name: hackathon-service
description: Helm chart for deploying hackathon team services to EKS
type: application
version: 1.0.0
appVersion: "1.0"

# Add your service dependencies here (optional)
# Example:
dependencies:
  - name: mssql
    version: 1.2.0
    repository: oci://registry.gitlab.com/xrow-public/helm-mssql/charts
    condition: mssql.enabled
#   - name: redis
#     version: "17.3.0"
#     repository: "https://charts.bitnami.com/bitnami"
#     condition: redis.enabled
